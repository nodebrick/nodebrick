{
    "name": "@nodebrick/nodebrick-helloworld",
    "version": "2.0.0",
    "description": "Nodebrick core, the glue between all the other modules",
    "main": "index.ts",
    "repository": "https://github.com/nodebrick",
    "author": "Xavier Martin",
    "license": "MIT",
    "private": false,
    "scripts": {
        "clean": "rm -Rf ./dist *.tgz",
        "build": "yarn clean && tsc && cp package.json DEV.README.md README.md",
        "lint": "eslint ./src/**/*.ts ./tests/**/*.ts && tsc --noEmit",
        "test:dev": "",
        "test:ci": "",
        "start": "ts-node-dev --inspect=0.0.0.0:${DEBUG_PORT} tests/playground.ts --inline"
    },
    "dependencies": {
        "@nodebrick/nodebrick-core": "2.0.0"
    }
}